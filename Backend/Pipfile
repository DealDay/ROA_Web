[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.65.1"
uvicorn = ">=0.14.0"
motor = ">=2.4.0"
pyjwt = ">=1.7.1"
python-decouple = ">=3.3"
django-email = "==0.1.10"
pydantic = {extras = ["email"], version = "*"}

[dev-packages]

[requires]
python_version = "3.10"
