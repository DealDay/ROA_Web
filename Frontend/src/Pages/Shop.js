import React, { useState, useEffect } from "react";
import axios from "axios";

const Shop = () => {
  const [bags, setBags] = useState([{}])

  //get all bags from db
  // useEffect(() =>{
  //   axios.post('http://localhost:8000/user/login').then(res => {
  //   setBags(res.data)
  //   })
  // });
  return (
    <input type="text"></input>
  )
};

export default Shop;
